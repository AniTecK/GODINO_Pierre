{% extends 'base.html.twig' %}

{% block title %}Modification du livre n°{{ livre.id}}{% endblock %}

{% block body %}
    <div class="container m-auto">
        <h1 class="text-center">Modification du livre n°{{ livre.id}}</h1>

        {{ include('livre/_form.html.twig', {'button_label': 'Enregistrer'}) }}

        <a href="{{ path('livre_index') }}" class="btn btn-info mb-2 mt-2">Retourner à la liste</a>

        {{ include('livre/_delete_form.html.twig') }}
    </div> 
{% endblock %}
