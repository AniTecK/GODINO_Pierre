{% extends 'base.html.twig' %}

{% block title %}Ajouter un livre{% endblock %}

{% block body %}
    <div class="container m-auto">
        <h1>Nouveau livre</h1>

        {{ include('livre/_form.html.twig') }}

        <a href="{{ path('livre_index') }}" class="btn btn-info mt-2">Retourner à la liste</a>
    </div>
{% endblock %}
